import Header from "../common/Header";
import Section from "../common/Section";

const Past = () => {
  return (
    <Section>
      <Header>Previously At ...</Header>;
    </Section>
  );
};

export default Past;
